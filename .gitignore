# --- Python ---
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# --- macOS ---
.DS_Store

# --- Claude Code local session state ---
.claude/

# --- Secrets / local env ---
.env

# --- YOLO: runtime-generated data, not source ---
YOLO/captured_images/
YOLO/detection_results/
YOLO/calibration_images/
YOLO/output.log
YOLO/updated_movement_data_test.json.txt

# --- Old/backup copies of YOLO kept around locally, not meant to be tracked ---
yolo_backup/
YOLO_old/

# --- MVS: vendor SDK installer binaries (Hikvision), not source; download
#     separately per YOLO/README.txt / camera_capture.py rather than tracking
#     ~120MB of .deb/.tar.gz in git ---
MVS/*.deb
MVS/*.tar.gz
